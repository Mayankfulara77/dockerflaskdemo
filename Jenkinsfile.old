pipeline{
    agent any
    tools {
        git 'Default'
    }
    stages {
        stage('Git checkout') {
           steps{
                git branch: 'main', credentialsId: 'Github', url: 'https://github.com/Mayankfulara77/dockerflaskdemo.git'
            }
        }

        stage('Build ImG From Docker file'){
            sh 'docker build -t name DockerDemoImg'
        }
        stage('Creating Container'){
            sh 'docker run -it -p 9090:9090 -d --name MyWorkCont'
        }
        stage('Working'){
            sh 'echo "done"'
        }
    }
}
